/* Badge is the original name for the same pill. It stays a straight alias so
   every existing import — component and tone type — keeps working with no
   behavioural drift between the two names. */
export { Pill as Badge } from "./Pill";
export type { PillTone as BadgeTone } from "./Pill";
